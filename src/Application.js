export default class Application {
  /**
   * Main entry point for the application. Feel free to modify this code as needed. It does not need to remain async, if you decide to pass dependencies in.
   */
  static async run() {
    console.log('Starting application...');
  }
}
